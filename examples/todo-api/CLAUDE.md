# 할 일(todo) 웹앱 — 프로젝트 가이드

## 개요
Flask 기반의 간단한 할 일 관리 API + 웹 UI.

## 실행
- 서버: `flask --app app run`
- 테스트: `pytest -q`

## 규칙
- 모든 새 라우트는 테스트를 먼저 작성한다(TDD).
- 응답은 JSON, 한글 메시지를 사용한다.
- 커밋 메시지는 한 줄 요약 + 본문으로 작성한다.
